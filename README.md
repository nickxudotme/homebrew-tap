# Nick Xu Homebrew Tap

Homebrew tap for small macOS tools.

## ScreenOff

Install the app and CLI:

```sh
brew install --cask nickxudotme/tap/screenoff
```

Or tap first:

```sh
brew tap nickxudotme/tap
brew install --cask screenoff
```

ScreenOff installs:

- `ScreenOff.app`
- `screenoff` CLI
